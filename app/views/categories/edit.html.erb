<% content_for :title, "Editar categoria — LifeHub Admin" %>

<%= link_to "← Voltar a Categorias", categories_path, class: "text-sm text-slate-500 hover:text-slate-900" %>
<h1 class="text-2xl font-semibold text-slate-900 mt-2 mb-6">Editar categoria</h1>

<%= render "form", category: @category, url: category_path(params[:id]), method: :patch %>
