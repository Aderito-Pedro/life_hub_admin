<% content_for :title, "Editar produto — LifeHub Admin" %>

<%= link_to "← Voltar a Produtos", products_path, class: "text-sm text-slate-500 hover:text-slate-900" %>
<h1 class="text-2xl font-semibold text-slate-900 mt-2 mb-6">Editar produto</h1>

<%= render "form", product: @product, url: product_path(params[:id]), method: :patch %>
