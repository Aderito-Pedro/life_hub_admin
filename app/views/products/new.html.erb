<% content_for :title, "Novo produto — LifeHub Admin" %>

<%= link_to "← Voltar a Produtos", products_path, class: "text-sm text-slate-500 hover:text-slate-900" %>
<h1 class="text-2xl font-semibold text-slate-900 mt-2 mb-6">Novo produto</h1>

<%= render "form", product: @product, url: products_path, method: :post %>
